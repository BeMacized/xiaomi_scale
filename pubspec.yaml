name: xiaomi_scale
description: A Flutter plugin for taking measurements from Bluetooth Xiaomi weight scales
version: 1.0.2
repository: https://github.com/BeMacized/xiaomi_scale
issue_tracker: https://github.com/BeMacized/xiaomi_scale/issues
homepage: https://pub.dev/packages/xiaomi_scale/

environment:
  sdk: ">=2.5.0 <3.0.0"
  flutter: ">=1.10.0"

dependencies:
  rxdart: ^0.24.1
  flutter_reactive_ble: ^2.3.0
  uuid: ^2.2.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

dartdoc:
  include: ['xiaomi_scale']

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:
